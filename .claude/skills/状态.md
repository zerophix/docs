---
name: 状态
description: 查看 Learning Log 系统状态。当用户输入 /状态 或问 学习状态 时触发。
allowed-tools: Bash(learnlog *)
---

# 学习日志状态速查

```bash
learnlog status
learnlog feed -n 5
```

前后端由 launchd 持久化运行（关闭 IDE/终端不受影响）。
如服务异常，执行 `learnlog service status` 排查。
